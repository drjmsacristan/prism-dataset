# PRISM — pinned to the versions used to produce the released results.
# Feature extraction:
lief==0.14.1
numpy>=1.24,<2.5
# Baselines / benchmark:
lightgbm>=4.0
scikit-learn>=1.3
scipy>=1.11
pandas>=2.0
# Figures:
matplotlib>=3.7
# EMBER cross-representation baseline (B4) only:
#   The EMBER v2 featurizer ('ember') was developed for LIEF 0.9 and is run
#   here under LIEF 0.14 with two compatibility monkey-patches applied at
#   runtime (see benchmark/ember_extract_bodmas.py):
#     (a) numpy type aliases (np.int/float/bool/... -> builtins)
#     (b) LIEF exception classes (bad_format/bad_file/pe_error/... -> Exception)
#   Install EMBER from its upstream repository if you wish to reproduce B4.
